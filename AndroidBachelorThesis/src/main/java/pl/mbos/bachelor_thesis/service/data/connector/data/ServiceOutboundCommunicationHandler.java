package pl.mbos.bachelor_thesis.service.data.connector.data;

import android.os.Handler;
import android.os.Message;

/**
 * Handles sending communicates to {@link pl.mbos.bachelor_thesis.service.data.DataService}
 */
public class ServiceOutboundCommunicationHandler extends Handler {

    @Override
    public void handleMessage(Message msg) {
        super.handleMessage(msg);
    }

}
